# RAG Petergof API

Система RAG (Retrieval-Augmented Generation) для работы с историческими документами о Петергофе. Проект предоставляет API и веб-интерфейс для поиска информации в документах с использованием семантического поиска (FAISS) и полнотекстового поиска (BM25), а также генерации ответов на основе найденных документов с помощью Yandex GPT.

## 🚀 Основные возможности

- **Семантический поиск**: Использование FAISS для векторного поиска по эмбеддингам
- **Полнотекстовый поиск**: BM25 для классического текстового поиска
- **OCR обработка**: Автоматическое распознавание текста из PDF документов
- **Генерация ответов**: Интеграция с Yandex GPT для создания ответов на основе найденных документов
- **Управление индексами**: Создание и управление поисковыми индексами
- **Аутентификация**: JWT-токены для безопасного доступа
- **Настройки пользователя**: Персонализация параметров поиска и генерации
- **Статистика**: Отслеживание использования системы

## 📋 Структура проекта

```
RAG_Petergof_API/
├── API/                    # Backend приложение (FastAPI)
│   ├── app/
│   │   ├── api/v1/        # API endpoints
│   │   ├── core/          # Конфигурация и зависимости
│   │   ├── db/            # Модели БД и схемы
│   │   └── main.py        # Точка входа приложения
│   ├── rag/               # RAG логика
│   ├── ocr/               # OCR обработка
│   ├── alembic/           # Миграции БД
│   └── requirements.txt   # Python зависимости
├── frontend/              # Frontend приложение (React)
│   ├── src/
│   │   ├── api/           # API клиенты
│   │   └── components/    # React компоненты
│   └── package.json       # Node.js зависимости
└── docker-compose.yaml    # Docker конфигурация
```

## 🛠 Технологический стек

### Backend
- **FastAPI** - веб-фреймворк
- **PostgreSQL** - база данных
- **SQLAlchemy** - ORM
- **Alembic** - миграции БД
- **FAISS** - векторный поиск
- **sentence-transformers** - создание эмбеддингов
- **rank-bm25** - полнотекстовый поиск
- **Yandex Cloud ML SDK** - интеграция с Yandex GPT
- **PyJWT** - JWT токены
- **aiobotocore** - работа с Yandex Object Storage

### Frontend
- **React** - UI библиотека
- **React Router** - маршрутизация
- **React Bootstrap** - UI компоненты
- **Axios** - HTTP клиент

## 📦 Установка и запуск

### Предварительные требования

- Docker и Docker Compose
- Yandex Cloud аккаунт с настроенными:
  - API ключ для Yandex GPT
  - Object Storage (S3) с bucket для хранения индексов
  - Доступ к Yandex Cloud CLI

### Настройка окружения

1. Создайте файл `.env` в корне проекта:

```env
# База данных
DATABASE_URL=postgresql+asyncpg://andrey:Password@postgres:5432/rag_database

# JWT токены
SECRET_KEY=your-secret-key-here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Yandex Cloud
YC_API_KEY=your-yandex-cloud-api-key
YC_FOLDER_ID=your-folder-id
ACCESS_KEY=your-s3-access-key
SECRET_KEY_OCR=your-s3-secret-key
```

2. Настройте Yandex Cloud CLI (для API контейнера):
   - Убедитесь, что директория `~/.config/yandex-cloud` содержит конфигурацию

### Запуск с Docker Compose

```bash
# Запуск всех сервисов
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка сервисов
docker-compose down
```

После запуска:
- **API**: http://localhost:8000
- **API документация (Swagger)**: http://localhost:8000/docs
- **API документация (ReDoc)**: http://localhost:8000/redoc
- **Frontend**: http://localhost:3000
- **PostgreSQL**: localhost:5432

### Ручная установка (без Docker)

#### Backend

```bash
cd API
pip install -r requirements.txt

# Настройка базы данных
alembic upgrade head

# Запуск сервера
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

#### Frontend

```bash
cd frontend
npm install
npm start
```

## 📚 API Документация и Endpoints

FastAPI автоматически генерирует интерактивную документацию:
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

### Аутентификация

### Аутентификация
- `POST /api/v1/token` - Получение токенов доступа
- `POST /api/v1/refresh` - Обновление токена
- `POST /api/v1/register` - Регистрация пользователя
- `GET /api/v1/me` - Информация о текущем пользователе

### RAG
- `POST /api/v1/rag/answer` - Задать вопрос (возвращает task_id)
- `GET /api/v1/rag/answer/status/{task_id}` - Проверить статус ответа

### Индексы
- `GET /api/v1/indexes` - Получить список индексов
- `POST /api/v1/indexes` - Создать новый индекс (только для админов)
- `GET /api/v1/indexes/status` - Статус создания индекса

### Файлы
- `GET /api/v1/files` - Получить список файлов (только для админов)
- `POST /api/v1/files` - Загрузить PDF для OCR (только для админов)
- `GET /api/v1/files/status` - Статус OCR обработки

### Настройки
- `GET /api/v1/settings` - Получить настройки пользователя
- `POST /api/v1/settings` - Обновить настройки пользователя

### Статистика
- `POST /api/v1/review` - Отправить отзыв/статистику использования

## 🔧 Настройки пользователя

Каждый пользователь может настроить параметры поиска и генерации:

- **temperature** - температура для генерации ответов (0.0-1.0)
- **count_vector** - количество результатов FAISS поиска
- **count_fulltext** - количество результатов BM25 поиска
- **prompt** - промпт для генерации ответов

## 👤 Административные функции

Пользователи с правами администратора могут:
- Загружать PDF файлы для OCR обработки
- Создавать новые поисковые индексы
- Просматривать список всех файлов
- Управлять статусом обработки

## 🗄 База данных

Проект использует PostgreSQL с следующими основными таблицами:
- **users** - пользователи системы
- **user_settings** - настройки пользователей
- **reviews** - отзывы и статистика использования

Миграции выполняются автоматически при запуске через Alembic.

## 📝 Работа с индексами

Индексы хранятся в Yandex Object Storage и содержат:
- FAISS индекс для векторного поиска
- BM25 индекс для полнотекстового поиска
- Метаданные о чанках (файл, страница)
- Эмбеддинги документов

Процесс создания индекса:
1. Загрузка PDF файлов через OCR
2. Обработка текста и разбиение на чанки
3. Создание эмбеддингов
4. Построение FAISS и BM25 индексов
5. Сохранение в Object Storage

## 🔐 Безопасность

- JWT токены для аутентификации
- Хеширование паролей
- Разделение прав доступа (обычные пользователи / администраторы)
- CORS настройки для безопасного взаимодействия frontend и backend

## 📄 Лицензия

[Укажите лицензию проекта]

## 👥 Авторы

[Укажите авторов проекта]

## 🤝 Вклад в проект

[Укажите информацию о том, как можно внести вклад в проект]

